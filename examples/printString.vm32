.SEGMENT code
.privLvl 0
.GLOBAL startup

startup:
	MOV (strPtr), sayHello
	MOV ds:(counter), 0

next:
	CMP ds:[strPtr], 0
	JZ end
	PRINT ds:[strPtr]
	ADD ds:(strPtr), 1
	JMP next

end:
	JMP end

.SEGMENT data
sayHello:
	.string "Hello, World!"

strPtr:
	.alloc 1
counter:
	.alloc 1
