.SEGMENT code
.privLvl 0
.GLOBAL startup

startup:
	MOV (strPtr), sayHello
	MOV (counter), 0

next:
	CMP [strPtr], 0
	JZ end
	PRINT [strPtr]
	ADD (strPtr), 1
	JMP next

end:
	JMP end

.SEGMENT data
sayHello:
	.string "Hello, World!"

strPtr:
	.alloc 1
counter:
	.alloc 1
